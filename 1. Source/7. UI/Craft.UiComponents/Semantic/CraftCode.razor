@inherits CraftComponent

<code @ref="ElementRef"
      id="@GetId()"
      class="@BuildCssClass()"
      style="@BuildStyle()"
      @attributes="@UserAttributes"
      @onclick="HandleClickAsync">@if (!string.IsNullOrEmpty(Content))
    {@Content}
    else
    {@ChildContent}
</code>
